

checkIfLoggedInUser() async {
  // String? userId = await LocalDatabase.getSecuredString(Constants.userId);

  // if (!userId.isNullOrEmpty()) {
  //   isLoggedInUser = true;
  // } else {
  //   isLoggedInUser = false;
  // }
}
